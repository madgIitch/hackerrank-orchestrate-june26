# Evaluation Report — Multi-Modal Evidence Review

_This report is generated by `python code/evaluation/main.py --version both`._
_Requires: `GEMINI_API_KEY` and `GEMINI_MODEL` environment variables._

## Status

Evaluation not yet run against real model. Run the evaluation script to populate this report with real numbers:

```bash
export GEMINI_API_KEY=<your-key>
export GEMINI_MODEL=gemini-2.0-flash
python code/evaluation/main.py --version both
```

## Prompt versions

| Version | File | Description |
|---|---|---|
| v1 (baseline) | `prompts/system_prompt.txt` | Original prompt from feature 3 |
| v2 (current) | `prompts/system_prompt_v2.txt` | Improved prompt with explicit claim_status rules, image ID grounding, two-pass pipeline |

## Metrics to be reported

- **claim_status accuracy** (primary): % of rows where predicted claim_status matches sample ground truth
- **issue_type accuracy**: % matching
- **object_part accuracy**: % matching
- **supporting_image_ids coherence**: count of supported rows with supporting_image_ids=none (should be 0)

## Expected improvements in v2

- Reduced false `supported` via evidence_standard_met=false post-process override
- Foreign image ID filtering prevents cross-claim contamination
- Two-pass pipeline provides more accurate evidence_requirements lookup
- Explicit claim_status rules reduce ambiguous `contradicted` decisions
